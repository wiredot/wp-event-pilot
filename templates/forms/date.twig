<div class="field">
	<label>{{ field.label }}</label>
	<input type="date" name="{{ field.id }}" placeholder="{{ field.label }}" required="required"{% if value is defined %}value="{{ value }}"{% endif %}>
</div>